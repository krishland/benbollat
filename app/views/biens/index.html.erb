<h2>Index</h2>

<div class="container">
  <% @biens.each do |bien| %>
  <div class="cards">
    <%= bien.address %>
    <%= bien.loyé%>
    <%#= bien.picture%>
    <%#=bien.meublé?%>
    <%#=bien.saisonnié?%>
    <%#=bien.loué?%>
  </div>
  <% end %>
</div>

  <div class="row">
    <div class="col-6">
    <ul class="cards-map">
      <% @biens.each do |bien| %>
        <div class="card-product">
          <%= bien.address %>
          <%= bien.loyé%>
        </div>
        <br>
      <% end %>
    </ul>
    </div>
